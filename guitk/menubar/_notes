+   delete doesn't always work... why not?

+   what about hs.menubar's callback to return dynamic JIT menu? best way to mimic that?
+       use `update` stage of menu callback and invoke `menu:removeAll()` before new menu built

+   popup menu needs to take into account screen is flipped

    drag-and-drop needs to be added

    document

    crap-ton-o-testing

    wrapper with metatables for table like use

    wrapper to replace `hs.menubar`

    some special keys don't seem to be caught; need to see if we can fix that or make list of them and remove from table

    can key equivalents work when menu not open yet?
